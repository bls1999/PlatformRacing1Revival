#ifndef RACEHANDLER_H
#define RACEHANDLER_H

#include "raceInstance.h"
#include <vector>

struct raceHandler{

    std::vector<unsigned int> emptyRaces;
    std::vector<raceInstance> currentRaces;

};

#endif
